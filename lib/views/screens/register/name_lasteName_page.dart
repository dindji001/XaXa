import 'package:flutter/material.dart';

class NameLastenamePage extends StatelessWidget {
  const NameLastenamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('page pour le nom et prenom de l\'utilisateuer'),),);
  }
}